[project]
name = "sqlalchemy-demo"
version = "0.1.0"
description = "ProTest factory fixtures with SQLAlchemy"
requires-python = ">=3.11"
dependencies = [
    "protest",
    "sqlalchemy>=2.0",
]

[tool.uv.sources]
protest = { path = "../.." }
